// Switch is a binary on/off control with no discrete style variants in Figma
// (node 74:11) — its visual states are driven entirely by the `checked` and
// `disabled` props against component tokens, so no cva() map is needed here.
export {};
